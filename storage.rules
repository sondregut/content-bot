rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Deny all client access — only Admin SDK (server) uploads/reads.
    // Clients receive signed URLs for reading images.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
